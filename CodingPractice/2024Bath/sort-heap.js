// todo


array = [1, 3, 5, 2, 55, 22, 12, 34 ,26 , 64 , 36 , 54 , 32 , 34 ]

function heapSort(arr){
    size = arr.length
    array.forEach(( el , index) => {
        let lcIndex = index*2
        let rcIndex = index * 2 + 1
        if( lcIndex < size && rcIndex < size){
            let lc = arr[lcIndex]
            let rc = arr[rcIndex]

            // while()
        }
    });

}


function insertInHeap(arr, item){

}